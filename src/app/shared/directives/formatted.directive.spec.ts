import { FormattedDirective } from './formatted.directive';

describe('FormattedDirective', () => {
  it('should create an instance', () => {
    const directive = new FormattedDirective();
    expect(directive).toBeTruthy();
  });
});
